# Legacy Python Code

This directory contains the original Python implementation of afk, archived for reference only.

**Do not use this code.** The project has been completely rewritten in Rust for improved performance and easier distribution.

## Contents

- `python-src/` — Original Python source code
- `python-tests/` — Original Python test suite
- `pyproject.toml` — Python package configuration
- `Makefile` — Python build commands

## Current Version

See the root of the repository for the active Rust implementation.
